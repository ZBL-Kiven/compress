<resources>

    <!-- Base application theme. -->
    <style name="Theme.ZCompress" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/pg_color_primary</item>
        <item name="colorPrimaryDark">@color/pg_color_primary</item>
        <item name="colorAccent">@color/pg_color_primary</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>
